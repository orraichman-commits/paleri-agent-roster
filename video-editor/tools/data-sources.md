# Tools / Data Sources — Video Editor

Authoritative Inputs. Read fresh; never assume.

- **tasks.input_data.shared_context.upstream_outputs** — the creative brief (Creative
  Strategist), source assets (Visual Producer), hooks/script (Copywriter);
  **missing_upstream** for anything not delivered.
- **tasks.input_data** — the task instruction.
- **Training Room** — brand rules for pacing, captions, logo/end-card usage (via Knowledge Agent).

Render connectors are used only when connected and approved; otherwise deliver an edit
plan / EDL. Output is written to **tasks.output_data** and routed to Level 1 approval.
